SHOPLINE 应用审核流程​
当你的应用完成开发和测试后，可以提交申请审核上架到 SHOPLINE的应用商店。
提交审核申请后，SHOPLINE 将对你所提交的应用信息进行审核，若通过审核，你的应用将可展示在 SHOPLINE 应用市场。

应用审核流程​
image.png你的应用在审核流程中会经历草稿、审核中和上线三个状态：

草稿：当创建一个应用后，应用的初始状态为「草稿」。在此阶段，开发者可对应用进行编辑、开发与测试。
审核中：当提交审核申请后，应用状态将变为「审核中」。在此阶段，SHOPLINE 将对应用进行审核.
若审核通过，则应用将上架至 SHOPLINE 应用市场，应用状态变为上线；
若审核不通过，应用状态将变为「审核不通过」，并通过邮件告知失败原因，开发者需要根据失败原因，补充应用信息进行重新申请。
上线：你的应用审核通过，已经上架到 SHOPLINE 应用市场，你的应用状态会变为「上线」。